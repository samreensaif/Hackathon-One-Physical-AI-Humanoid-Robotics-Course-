# ── OpenAI ────────────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# ── Qdrant Cloud (free tier) ──────────────────────────────────────────────────
# Found in: Qdrant Cloud Dashboard → Cluster → API Keys
QDRANT_URL=https://your-cluster-id.eu-central-1-0.aws.cloud.qdrant.io:6333
QDRANT_API_KEY=your-qdrant-api-key-here
QDRANT_COLLECTION_NAME=textbook_chunks

# ── Neon Serverless Postgres ──────────────────────────────────────────────────
# Found in: Neon Dashboard → Connection Details → Connection string
# Must include ?sslmode=require
NEON_DATABASE_URL=postgresql://user:password@your-host.neon.tech/neondb?sslmode=require

# ── Textbook Content ─────────────────────────────────────────────────────────
# Path to the /docs directory of the Docusaurus textbook.
# Can be relative (resolved from the chatbot-backend/ directory) or absolute.
TEXTBOOK_DOCS_PATH=../physical-ai-textbook/docs

# ── CORS ─────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed origins.
# Update with your GitHub Pages URL after deployment.
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://samreensaif.github.io

# ── RAG Parameters (optional — defaults shown) ────────────────────────────────
# These override the defaults in config.py; remove any line to use the default.
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_DIMENSIONS=1536
# CHAT_MODEL=gpt-4o-mini
# TOP_K_CHUNKS=5           # Chunks retrieved from Qdrant per query
# MAX_CHUNK_CHARS=1500     # Max characters per text chunk during ingest
# CHUNK_OVERLAP_CHARS=150  # Overlap between consecutive chunks
# CHAT_HISTORY_TURNS=6     # Recent conversation turns included in each prompt
# MAX_CHAT_TOKENS=1024     # Max tokens for the chat completion response
